---
name: User profile — <operator-user>
description: MicroStrategy (Strategy) insider evaluating/extending Mosaic as a semantic-layer offering; wants end-to-end REST API fluency in Claude.
type: user
originSessionId: cef55f31-c57d-4220-b4dc-eddfff684771
---
- Works closely with MicroStrategy / Strategy (studio.strategy.com tenant, user `<operator-user>`).
- Treats **Strategy Mosaic** = the canonical semantic layer for business data; the goal of most sessions is to make Mosaic programmable, testable, and benchmark-worthy against alternatives (Snowflake Semantic Views, Cortex, raw SQL agents).
- Expects Claude to have full-stack knowledge of the Mosaic metadata model — datasources, physical/logical tables, attributes (+ all form types), facts, every metric shape (simple, compound, conditional, level, transformation-based), filters, consolidations, custom groups, prompts, hierarchies, security filters/ACLs, translations, VLDB.
- Style: prefers terse updates; wants working code that runs end-to-end on `studio.strategy.com`, not pseudocode. Happy with concrete sample calls; allergic to "here's how it *might* work" prose.
- Comfortable when Claude iteratively probes the REST API and updates skill/memory in place as it learns — does not want brittle hardcoded paths.
